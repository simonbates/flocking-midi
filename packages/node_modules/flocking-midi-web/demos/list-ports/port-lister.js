/*
 * Flocking MIDI Demos: Port Lister
 * http://github.com/colinbdclark/flocking-midi
 *
 * Copyright 2016, Colin Clark
 * Licensed under the MIT license.
 */

"use strict";

fluid.defaults("flock.midi.demos.portLister", {
    gradeNames: "fluid.component",

    components: {
        portsView: {
            type: "flock.midi.demos.portsView",
            container: "main",
            options: {
                model: "{system}.model"
            }
        },

        system: {
            type: "flock.midi.system"
        }
    },

    listeners: {
        "onCreate.requestAccess": "{system}.access.requestAccess()"
    }
});
