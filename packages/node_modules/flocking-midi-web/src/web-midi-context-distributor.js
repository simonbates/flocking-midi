// Unsupported, non-API function.
fluid.defaults("flock.midi.web.contextDistributor", {
    gradeNames: "fluid.component",

    distributeOptions: [
        {
            target: "{/ flock.midi.system}.options",
            record: {
                gradeNames: ["flock.midi.web.system"]
            }
        },
        {
            target: "{/ flock.midi.system > access}.options",
            record: {
                gradeNames: ["flock.midi.web.access"]
            }
        }
    ]
});

fluid.constructSingle([], {
    singleRootType: "flock.midi.contextDistributor",
    type: "flock.midi.web.contextDistributor"
});
