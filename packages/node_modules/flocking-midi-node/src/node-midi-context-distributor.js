// TODO: RAMPANT CUT AND PASTAGE
var fluid = require("infusion");

// Unsupported, non-API grade.
fluid.defaults("flock.midi.nodejs.contextDistributor", {
    gradeNames: "fluid.component",

    distributeOptions: [
        {
            target: "{/ flock.midi.system}.options",
            record: {
                gradeNames: ["flock.midi.nodejs.system"]
            }
        },
        {
            target: "{/ flock.midi.system > access}.options",
            record: {
                gradeNames: ["flock.midi.nodejs.access"]
            }
        }
    ]
});

fluid.constructSingle([], {
    singleRootType: "flock.midi.contextDistributor",
    type: "flock.midi.nodejs.contextDistributor"
});
